interface:
  display_name: "UI Visual Design Workflow"
  short_description: "Guide beginner visual designers"
  default_prompt: "Use $ui-visual-design-workflow in lightweight mode by default. Use $imagegen only when final bitmap output is explicitly requested."
